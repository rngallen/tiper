# Development only. The Vite dev server proxies /api to this origin so the
# HttpOnly session cookies set by the Go API stay first-party.
VITE_API_PROXY_TARGET=http://127.0.0.1:8080
# Optional: shown in the browser tab / login page.
VITE_APP_NAME=TIPER DFMS
